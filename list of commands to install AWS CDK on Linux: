sudo apt-get update
sudo apt-get install -y nodejs npm
sudo npm install -g aws-cdk
cdk --version
mkdir my-cdk-project
cd my-cdk-project
cdk init app --language=typescript
cdk bootstrap

These commands will install and set up AWS CDK on a Linux system, enabling you to start building infrastructure as code using AWS CDK
